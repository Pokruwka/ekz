/*ООП в С++. Основные понятия
Объектно-ориентированное программирование — это подход, основанный на моделировании реальных объектов через классы и объекты. В C++ ООП позволяет создавать программы, которые легче понимать, модифицировать и расширять.

 1. Объявление класса

Что такое класс?
Класс — это шаблон или схема для создания объектов. Класс объединяет данные (поля) и действия над ними (методы).

Синтаксис объявления класса
class ИмяКласса {
private:
    // Поля и методы доступны только внутри класса
public:
    // Поля и методы доступны извне
protected:
    // Поля и методы доступны только наследникам и классу
};

Пример:
class Car {
private:
    string brand;   // Поле
    int speed;      // Поле

public:
    void setBrand(string b) { // Метод
        brand = b;
    }
    string getBrand() {       // Метод
        return brand;
    }
};

- `class` — ключевое слово для объявления класса.
- ИмяКласса — имя класса, должно быть уникальным.
- Поля и методы разделяются квалификаторами доступа (`private`, public, `protected`).

2. Поля

Что такое поле класса?
Поле — это переменная, объявленная внутри класса, которая хранит данные объекта.

Характеристики полей:
- Поля могут быть любого типа: примитивного, пользовательского, указателем, массивом и т.д.
- Могут быть объявлены как статические или нестатические.

Инициализация полей:
1. Внутри конструктора:
     class Car {
   private:
       string brand;
       int speed;
   public:
       Car(string b, int s) {
           brand = b;
           speed = s;
       }
   };
   
2. С помощью списка инициализации:
     Car(string b, int s) : brand(b), speed(s) {}
   
3. При объявлении (начиная с C++11):
     class Car {
   private:
       string brand = "Unknown";
       int speed = 0;
   };

3. Методы

Что такое метод?
Метод — это функция, объявленная внутри класса, которая работает с данными класса.

Объявление метода
- Объявляется внутри класса, а его реализация может быть внутри или вне класса.
- Может работать с полями класса напрямую или через параметры.

Синтаксис:
тип_возвращаемого_значения имя_метода(параметры);

Пример реализации внутри класса:
class Car {
public:
    void start() {
        cout << "Car started!" << endl;
    }
};

Пример реализации вне класса:
class Car {
public:
    void start(); // Объявление
};

void Car::start() { // Определение
    cout << "Car started!" << endl;
}

Типы методов
1. Статические методы: 
   - Вызываются без создания объекта. 
   - Объявляются с ключевым словом static.
     class Math {
   public:
       static int add(int a, int b) {
           return a + b;
       }
   };

   int result = Math::add(3, 5);
   
2. Константные методы: 
   - Не изменяют поля объекта.
   - Объявляются с ключевым словом const.
     class Car {
   public:
       string getBrand() const {
           return brand;
       }
   };

4. Квалификаторы доступа

Квалификаторы доступа определяют, кто может получить доступ к полям и методам класса.

Виды квалификаторов:
1. `public`:
   - Поля и методы доступны везде.

2. `private`:
   - Поля и методы доступны только внутри класса.

3. `protected`:
   - Поля и методы доступны в классе и его наследниках.

Пример:
class Example {
private:
    int privateVar;  // Доступ только внутри класса
protected:
    int protectedVar; // Доступ внутри класса и наследников
public:
    int publicVar;   // Доступен везде
};

5. Устройство объекта в памяти

Как объект занимает память?
- Объект класса занимает память под свои поля.
- Методы занимают память только один раз для всего класса (если они не виртуальные).

Состав объекта в памяти:
1. Поля:
   - Каждое поле имеет собственный участок памяти.
   - Порядок размещения в памяти соответствует их объявлению.

2. Виртуальная таблица (vtable):
   - Для классов с виртуальными методами добавляется указатель на таблицу виртуальных функций.

3. Статические члены:
   - Статические поля и методы не хранятся в каждом объекте.
Они выделяются один раз в общей памяти для класса.

Пример анализа памяти:
class Example {
    int x;       // 4 байта (на 32-разрядной системе)
    double y;    // 8 байт
    static int z; // Статический член не входит в объект
};

Example obj;
- Размер объекта obj составит 12 байт (4 + 8), без учёта z.

Пример использования всех элементов:
#include <iostream>
#include <string>
using namespace std;

class Car {
private:
    string brand;
    int speed;

public:
    // Конструктор
    Car(string b, int s) : brand(b), speed(s) {}

    // Методы
    void setSpeed(int s) {
        speed = s;
    }

    int getSpeed() const {
        return speed;
    }

    void display() const {
        cout << "Brand: " << brand << ", Speed: " << speed << endl;
    }
};

int main() {
    Car car1("Toyota", 120); // Создание объекта
    car1.display();

    car1.setSpeed(150); // Изменение скорости
    car1.display();

    return 0;
}

Вывод:
Brand: Toyota, Speed: 120
Brand: Toyota, Speed: 150

Итоги:
1. Классы — основа ООП, объединяют данные и действия над ними.
2. Поля — переменные внутри класса, отвечают за данные объекта.
3. Методы — функции, работающие с полями и предоставляющие интерфейс.
4. Квалификаторы доступа определяют доступность полей и методов.
5. Объекты в памяти содержат только данные, методы общие для всех объектов.
*/