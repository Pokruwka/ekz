/*ООП в языке C++: Специальные члены функций
Специальные члены функций в C++ — это функции, которые автоматически создаются компилятором или могут быть определены программистом для управления созданием, копированием, перемещением и уничтожением объектов класса. Они включают:

1. Конструкторы.  
2. Конструктор копирования.  
3. Оператор присваивания копированием.  
4. Конструктор перемещения.  
5. Деструктор.

1. Конструкторы

Что такое конструктор?
Конструктор — это специальная функция класса, которая вызывается при создании объекта. Его основная задача — инициализация полей объекта.  

Особенности:
- Имя конструктора совпадает с именем класса.
- Конструктор не имеет возвращаемого значения.
- Если конструктор не определён, компилятор создаёт конструктор по умолчанию.

Типы конструкторов:

1. Конструктор по умолчанию:  	
   - Не принимает аргументов.
   - Инициализирует объект стандартными значениями.
     class Example {
       int x;
   public:
       Example() : x(0) {} // Конструктор по умолчанию
   };
   
2. Параметризованный конструктор:  
   - Принимает параметры для инициализации полей.
     class Example {
       int x;
   public:
       Example(int value) : x(value) {} // Конструктор с параметром
   };
   
3. Конструктор с параметрами по умолчанию:  
   - Может быть вызван как с аргументами, так и без них.
     class Example {
       int x;
   public:
       Example(int value = 0) : x(value) {}
   };
   
4. Конструктор со списком инициализации:  
   - Поля инициализируются в списке перед телом конструктора.
     class Example {
       int x, y;
   public:
       Example(int a, int b) : x(a), y(b) {}
   };
   
5. Конструктор с перегрузкой:  
   - Один класс может иметь несколько конструкторов с разными параметрами.
     class Example {
   public:
       Example() {}
       Example(int a) {}
       Example(double b) {}
   };
   
---

2. Конструктор копирования

Что такое конструктор копирования?
Конструктор копирования создаёт новый объект как копию уже существующего.  

Синтаксис:
ClassName(const ClassName &obj);

Когда вызывается:
1. При передаче объекта в функцию по значению.
2. При возврате объекта из функции.
3. При явном создании копии объекта:
     ClassName obj1;
   ClassName obj2 = obj1; // Конструктор копирования
   
Пример конструктора копирования:
class Example {
    int x;
public:
    Example(int value) : x(value) {}
    Example(const Example &obj) : x(obj.x) {
        cout << "Copy constructor called" << endl;
    }
};

int main() {
    Example obj1(10);
    Example obj2 = obj1; // Конструктор копирования
    return 0;
}

3. Оператор присваивания копированием

 Что это?
Оператор присваивания копированием (`operator=`) отвечает за копирование содержимого одного объекта в другой уже существующий объект.  

Синтаксис:
ClassName& operator=(const ClassName &obj);

Пример реализации:
class Example {
    int x;
public:
    Example(int value) : x(value) {}
    Example& operator=(const Example &obj) {
        if (this != &obj) { // Проверка на самоприсваивание
            x = obj.x;
        }
        return *this;
    }
};

int main() {
    Example obj1(10), obj2(20);
    obj2 = obj1; // Присваивание копированием
    return 0;
}

---

4. Конструктор перемещения

Что такое конструктор перемещения?
Конструктор перемещения используется для передачи ресурсов от одного объекта к другому, минимизируя копирование.

Синтаксис:
ClassName(ClassName &&obj);

Когда вызывается:
1. При перемещении временных объектов (`std::move`).
2. При возврате объектов из функций (в некоторых случаях).

Пример конструктора перемещения:
class Example {
    int* data;
public:
    Example(int value) {
        data = new int(value);
    }

    Example(Example &&obj) : data(obj.data) { // Перемещаем данные
        obj.data = nullptr;
        cout << "Move constructor called" << endl;
    }

    ~Example() {
        delete data;
    }
};

int main() {
    Example obj1(10);
    Example obj2 = std::move(obj1); // Конструктор перемещения
    return 0;
}

---

5. Деструктор
Что такое деструктор?
Деструктор — это специальная функция, которая вызывается при уничтожении объекта. Основная задача — освобождение ресурсов, занимаемых объектом.

Особенности:
- Имя деструктора совпадает с именем класса, но с символом ~ перед ним.
- Не имеет параметров и возвращаемого значения.

Синтаксис:
~ClassName();

### Пример деструктора:
class Example {
    int* data;
public:
    Example(int value) {
        data = new int(value);
    }

    ~Example() {
        delete data; // Освобождаем динамическую память
        cout << "Destructor called" << endl;
    }
};

int main() {
    Example obj(10); // Деструктор будет вызван автоматически при завершении main()
    return 0;
}

Сравнение специального функционала
| Конструктор - Инициализация нового объекта. (ClassName obj);
| Конструктор копирования - Создание нового объекта как копии существующего. (ClassName obj2 = obj1); 
| Оператор присваивания - Присваивание значений одного объекта другому.  (obj2 = obj1);  
| Конструктор перемещения - Перемещение ресурсов от одного объекта к другому без копирования. (ClassName obj2 = std::move(obj));
| Деструктор - Освобождение ресурсов, занимаемых объектом.                                                         (Автоматически вызывается при уничтожении объекта). 

Итоги:
1. Конструкторы инициализируют объекты.
2. Конструктор копирования отвечает за создание копий.
3. Оператор присваивания управляет копированием между существующими объектами.
4. Конструктор перемещения позволяет эффективно управлять временными объектами.
5. Деструктор завершает жизненный цикл объекта, освобождая ресурсы.
*/