/*ООП С++. Полиморфизм
Полиморфизм (от греческого «много форм») — это один из ключевых принципов объектно-ориентированного программирования, который позволяет объектам разного типа обрабатывать одинаковые сообщения, обеспечивая единый интерфейс для взаимодействия.

1. Основная идея принципа полиморфизма

Цель полиморфизма:
1. Упрощение архитектуры программ:  
   Полиморфизм позволяет писать гибкий и переиспользуемый код, который может работать с объектами разных типов.
2. Унификация интерфейса:  
   Все объекты, связанные через полиморфизм, могут обрабатываться одинаково.
3. Инкапсуляция поведения:  
   Каждый класс определяет своё уникальное поведение, но внешнему коду доступен общий интерфейс.

Виды полиморфизма:
1. Компиляционный (или статический):  
   Решается на этапе компиляции. Примеры:
   - Перегрузка функций.
   - Перегрузка операторов.
2. Рантайм (или динамический):  
   Решается во время выполнения программы. Пример:
   - Виртуальные функции.


 2. Основные понятия

Полиморфизм в контексте наследования:
Полиморфизм часто связан с наследованием. Базовый класс предоставляет интерфейс, а производные классы реализуют или переопределяют поведение.

Ключевые понятия:
1. Виртуальная функция:  
   Метод базового класса, который может быть переопределён в производных классах.  
   Объявляется с помощью ключевого слова virtual.

2. Абстрактный класс:  
   Класс, который содержит хотя бы одну чисто виртуальную функцию. Используется для создания интерфейсов.  
     virtual void func() = 0;
   
3. Виртуальная таблица (vtable):  
   Внутренний механизм, который обеспечивает динамический полиморфизм. Каждый класс с виртуальными функциями имеет таблицу указателей на эти функции. Виртуальная таблица определяется на этапе компиляции, а выбор функции происходит во время выполнения.

4. Позднее связывание (Late Binding):  
   Выбор метода для вызова происходит на этапе выполнения, а не компиляции.

3. Реализация полиморфизма

3.1 Компиляционный полиморфизм (статический)

Перегрузка функций:
Функции с одинаковым именем, но с разными параметрами.  
Решение о вызове конкретной функции принимается на этапе компиляции.  class Printer {
public:
    void print(int value) {
        cout << "Integer: " << value << endl;
    }

    void print(double value) {
        cout << "Double: " << value << endl;
    }

    void print(string value) {
        cout << "String: " << value << endl;
    }
};

int main() {
    Printer p;
    p.print(42);        // Вывод: Integer: 42
    p.print(3.14);      // Вывод: Double: 3.14
    p.print("Hello");   // Вывод: String: Hello
    return 0;
}

Перегрузка операторов:
class Vector {
    int x, y;

public:
    Vector(int a, int b) : x(a), y(b) {}

    Vector operator+(const Vector &v) const {
        return Vector(x + v.x, y + v.y);
    }

    void display() const {
        cout << "Vector(" << x << ", " << y << ")" << endl;
    }
};

int main() {
    Vector v1(1, 2), v2(3, 4);
    Vector v3 = v1 + v2; // Используется перегруженный оператор "+"
    v3.display();        // Вывод: Vector(4, 6)
    return 0;
}

---

3.2 Ранний полиморфизм (наследование без виртуальных функций)

Если в классе не используются виртуальные функции, то выбор метода происходит на этапе компиляции.

Пример:
class Animal {
public:
    void speak() {
        cout << "Animal makes a sound" << endl;
    }
};

class Dog : public Animal {
public:
    void speak() {
        cout << "Dog barks" << endl;
    }
};

int main() {
    Animal a;
    Dog d;

    a.speak(); // Вывод: Animal makes a sound
    d.speak(); // Вывод: Dog barks

    Animal *ptr = &d;
    ptr->speak(); // Вывод: Animal makes a sound (нет виртуальности)
    return 0;
}

---

3.3 Динамический полиморфизм (виртуальные функции)

Пример с виртуальной функцией:
class Animal {
public:
    virtual void speak() {
        cout << "Animal makes a sound" << endl;
    }
};

class Dog : public Animal {
public:
    void speak() override {
        cout << "Dog barks" << endl;
    }
};

int main() {
	Animal *a = new Dog();
    a->speak(); // Вывод: Dog barks (динамическое связывание)
    delete a;
    return 0;
}

Как работает:
1. vtable:  
   Создаётся таблица указателей на виртуальные функции.
2. vptr:  
   Каждый объект класса с виртуальными функциями содержит указатель на соответствующую таблицу.
3. Динамический вызов:  
   Во время выполнения программы vtable используется для вызова правильной функции.

---

3.4 Абстрактные классы и интерфейсы

Абстрактный класс:
Абстрактный класс содержит хотя бы одну чисто виртуальную функцию. Экземпляр такого класса нельзя создать.

Пример:
class Shape {
public:
    virtual void draw() = 0; // Чисто виртуальная функция
};

class Circle : public Shape {
public:
    void draw() override {
        cout << "Drawing a circle" << endl;
    }
};

int main() {
    Shape *s = new Circle();
    s->draw(); // Вывод: Drawing a circle
    delete s;
    return 0;
}

Итоги:
1. Полиморфизм — это возможность объектов разных типов реагировать на одни и те же вызовы.
2. Разделяется на компиляционный (статический) и рантайм (динамический).
3. Компиляционный полиморфизм включает перегрузку функций и операторов.
4. Динамический полиморфизм реализуется с помощью виртуальных функций и наследования.
5. Полиморфизм упрощает расширение системы и делает её более гибкой.
*/