/*Поток исполнения программы на языке С++

1. Точка входа в программу

В C++ точкой входа в программу является функция main. Исполнение программы всегда начинается именно с этой функции. Она определяет основной поток выполнения.

- Возвращаемое значение:
  - int — стандартный тип возвращаемого значения функции main. Обычно возвращается 0 для успешного завершения программы.
  - Возвращаемое значение может использоваться операционной системой для определения статуса завершения программы.

 2. Операторы

Операторы в C++ — это специальные символы или ключевые слова, которые используются для выполнения операций над данными. Они являются основными строительными блоками для выражений и стейтментов.

Классификация операторов
1. Арифметические операторы:
   - +, -, *, /, % (остаток от деления).

2. Операторы присваивания:
   - =, +=, -=, *=, /=, %=.

3. Операторы сравнения (логические):
   - ==, !=, <, >, <=, >=.

4. Логические операторы:
   - && (логическое И), || (логическое ИЛИ), ! (логическое НЕ).

5. Побитовые операторы:
   - & (И), | (ИЛИ), ^ (исключающее ИЛИ), ~ (инверсия), << (сдвиг влево), >> (сдвиг вправо).

6. Операторы инкремента и декремента:
   - ++ (увеличение на 1), -- (уменьшение на 1).

7. Операторы работы с указателями:
   - & (адрес), * (разыменование).

8. Тернарный оператор:
   - ?: (условный выбор).

9. Операторы управления потоком:
   - sizeof, typeid.

Приоритет операторов
Операторы в C++ имеют определённый приоритет, который определяет порядок их выполнения в выражении. 

Пример приоритета:
int x = 10 + 5 * 2; // Сначала умножение, потом сложение: x = 20

- Приоритеты можно изменить с помощью круглых скобок ().
- Полный список приоритетов можно найти в документации. Важно знать, что арифметические операции выполняются первыми, а операции присваивания — последними.

3. Выражение

Что такое выражение?
Выражение (expression) — это комбинация значений, переменных и операторов, которая вычисляется в некоторое значение.

Примеры:
x + y;        // Выражение
a = b + c;    // Присваивающее выражение
x++;          // Инкремент

- Выражения могут быть использованы в более сложных конструкциях, таких как условия или циклы.

Порядок вычисления выражений
Порядок вычислений в выражениях определяется приоритетом операторов и их ассоциативностью:
- Левоассоциативные: +, -, *, / (выполняются слева направо).
- Правоассоциативные: = (выполняются справа налево).

4. Стейтмент

Что такое стейтмент?
Стейтмент (statement) — это законченное выражение, которое выполняет определённое действие. В отличие от выражений, стейтменты не обязательно возвращают значение.

Пример:
int x = 10; // Присваивающий стейтмент
cout << x;  // Вызов функции как стейтмент

Операторы выбора
Операторы выбора используются для выполнения различных ветвей кода в зависимости от условий.

1. if-else:
2. switch

Операторы цикла
Циклы повторяют выполнение кода, пока условие истинно.

1. for:
2. while:   
3. do-while:

Операторы перехода
Операторы перехода изменяют стандартный поток выполнения.

1. break: Прерывает выполнение цикла или оператора switch.
2. continue: Пропускает оставшуюся часть цикла и переходит к следующей итерации.
3. goto: Переходит к метке (используется редко из-за сложности сопровождения кода).
     goto label;
   label:
   // Код

5. Функция (с точки зрения влияния на поток исполнения программы)

Что такое функция?
Функция — это блок кода, выполняющий определённое действие. Она может быть вызвана из любой части программы.

Как функции влияют на поток исполнения?
- Когда функция вызывается, управление передаётся ей.
- После выполнения кода функции управление возвращается в точку вызова.

Типы функций:
1. Функции, возвращающие значение:
     int add(int a, int b) {
       return a + b;
   }
   
2. Функции без возвращаемого значения (`void`):
     void display() {
       cout << "Hello" << endl;
   }
   
3. Рекурсивные функции (влияют на поток через стек вызовов):
     int factorial(int n) {
       if (n == 0) return 1;
       return n * factorial(n - 1);
   }

Итоги:
- Точка входа в программу — это main.
- Операторы и выражения определяют вычисления и их порядок.
- Стейтменты задают структуру выполнения программы.
- Функции позволяют структурировать поток исполнения, делая код модульным.
*/