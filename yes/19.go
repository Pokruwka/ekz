/*Пользовательские типы данных  и структуры в Go
Go предоставляет возможность создавать пользовательские типы данных, которые упрощают работу с данными и повышают читаемость и структурированность кода. Ключевыми инструментами для этого являются структуры и методы.

1. Структуры (объявление, использование)

Структура (struct) — это пользовательский тип данных, объединяющий связанные переменные (поля) под одним именем. Поля структуры могут быть разных типов.

Объявление структуры

Синтаксис:
type ИмяСтруктуры struct {
    Поле1 Тип1
    Поле2 Тип2
    ...
}

- type — ключевое слово для объявления нового типа.
- ИмяСтруктуры — имя структуры (с заглавной буквы, если структура экспортируемая).
- Поле1, Поле2 — имена полей.
- Тип1, Тип2 — типы данных полей.

Пример:
type Person struct {
    Name string
    Age  int
}

Использование структуры

1. Создание экземпляра структуры:
   - Используйте литерал структуры.
   - Поля можно инициализировать по порядку или явно по имени.

Примеры:
// Полная инициализация
person1 := Person{"Alice", 30}

// Инициализация по имени полей
person2 := Person{Name: "Bob", Age: 25}

// Инициализация с пропуском некоторых полей (по умолчанию значения "нулевые")
person3 := Person{Name: "Eve"}

2. Обращение к полям:
   Поля структуры доступны через оператор . (точка).
     fmt.Println(person1.Name) // "Alice"
   person1.Age = 31          // Изменение значения поля
   
3. Вложенные структуры:
   Поля структуры могут быть структурами.
     type Address struct {
       City, Country string
   }

   type Person struct {
       Name    string
       Age     int
       Address Address
   }

   person := Person{
       Name: "Charlie",
       Age:  28,
       Address: Address{
           City:    "New York",
           Country: "USA",
       },
   }
   fmt.Println(person.Address.City) // "New York"
   
4. Сравнение структур:
   Две структуры одного типа можно сравнивать, если все их поля сравнимы.
     p1 := Person{Name: "Alice", Age: 30}
   p2 := Person{Name: "Alice", Age: 30}
   fmt.Println(p1 == p2) // true

2. Объявление типа

Объявление типа в Go позволяет создать новый тип на основе существующего.

Синтаксис:
type НовыйТип БазовыйТип

Пример:
type Celsius float64
type Kilometer int

Использование:
1. Присвоение значений:
     var temp Celsius = 36.6
   var distance Kilometer = 10
   
2. Ограниченная совместимость:
   - Новый тип считается отдельным, даже если базовый тип совпадает.
   - Для преобразования типов требуется явное приведение.
     var c Celsius = 36.6
   var f float64 = float64(c) // Преобразование

3. Методы (объявление, использование)

Методы в Go — это функции, которые привязываются к определённому типу (включая структуры). С помощью методов можно определять поведение пользовательских типов.

Объявление методов

Синтаксис:
func (receiver Тип) ИмяМетода(параметры) ВозвращаемыеТипы {
    // Тело метода
}
- receiver — параметр-получатель, к которому привязан метод. Это экземпляр структуры или пользовательского типа.
- Тип — тип данных получателя.
- ИмяМетода — имя метода.
- параметры и возвращаемыеТипы — такие же, как у обычных функций.

Пример:

1. Методы для структуры:
     type Person struct {
       Name string
       Age  int
   }

   // Метод для структуры Person
   func (p Person) Greet() {
       fmt.Printf("Hello, my name is %s and I am %d years old.\n", p.Name, p.Age)
   }

   func main() {
       p := Person{Name: "Alice", Age: 30}
       p.Greet() // Вывод: Hello, my name is Alice and I am 30 years old.
   }
   
2. Методы с изменением состояния:
   Чтобы метод мог изменять поля структуры, получатель должен быть указателем.
     func (p *Person) Birthday() {
       p.Age++
   }

   func main() {
       p := Person{Name: "Bob", Age: 25}
       p.Birthday()
       fmt.Println(p.Age) // 26
   }

Использование методов
Методы вызываются с помощью экземпляра структуры:p := Person{Name: "Eve", Age: 20}
p.Greet()

Итоги
Структуры - Пользовательский тип данных, объединяющий поля. Поля доступны через оператор.                                                                                                                                                
Объявление типа - Позволяет создавать новые типы на основе существующих. Типы совместимы только при явном преобразовании.                                                                                                                            
Методы - Функции, привязанные к типу (например, структуре). Методы с получателем-указателем могут изменять поля структуры.                                                                                                                  

*/
package main