/*Golang
Программа на языке Golang
Golang (или Go) — это современный компилируемый язык программирования, разработанный в Google. Он предназначен для создания высокопроизводительных, безопасных и простых в обслуживании программ. Рассмотрим подробно этапы работы с программой на Go.

1. Исходный код	

Исходный код программы на Go — это текстовый файл с расширением .go, содержащий инструкции, которые определяют поведение программы. 

Особенности исходного кода Go:

1. Структура файла:
   Каждый файл программы должен начинаться с объявления пакета:
     package main
      - `package main`: Указывает, что файл принадлежит главному пакету, который содержит точку входа программы.
   - Другие пакеты используются для разделения кода на модули, например: package math.

2. Точка входа:
   - В программе на Go основная функция — это main(). Она вызывается автоматически при запуске программы.
   - Пример минимальной программы:
         package main

     import "fmt" // Подключение стандартного пакета

     func main() {
         fmt.Println("Hello, Go!") // Вывод строки на экран
     }
     
3. Форматирование и стиль кода:
   - Go строго требует правильного форматирования кода. Для автоматического форматирования используется утилита gofmt.

4. Объявление функций:
   - Все функции в Go начинаются с ключевого слова func:
         func add(a int, b int) int {
         return a + b
     }
     
5. Импорты:
   - Go использует модульную систему. Импорты указываются в начале файла:
         import "math"

 2. Сборка программы

Go является компилируемым языком, поэтому исходный код необходимо преобразовать в исполняемый файл.

Этапы сборки:
1. Компиляция:
   - Go транслирует исходный код (`.go` файлы) в машинный код.
   - Для этого используется встроенная команда:
         go build
     
2. Создание исполняемого файла:
   - После выполнения go build создаётся исполняемый файл (без расширения в Linux/macOS и с .exe в Windows).

3. Поддержка кросс-компиляции:
   - Go позволяет компилировать программы для других операционных систем и архитектур. Например:
         GOOS=windows GOARCH=amd64 go build
          Это создаст исполняемый файл для Windows на системе Linux/macOS.

4. Пример сборки:
   - Пусть исходный код находится в файле main.go. Для сборки выполните:
         go build main.go
          В текущем каталоге появится исполняемый файл main.

5. Модули и зависимости:
   - Go использует систему модулей для управления зависимостями. Файл go.mod содержит метаинформацию о проекте:
         go mod init <module-name> # Инициализация модуля
     go get <package-name>    # Установка зависимостей

3. Запуск программы

Выполнение программы напрямую:
Go позволяет запускать программы без предварительной компиляции, используя команду:go run <файл>.go
Пример:
go run main.go
- Команда компилирует исходный код во временный исполняемый файл, запускает его и удаляет после завершения.

Запуск собранного исполняемого файла:
После компиляции с помощью go build, исполняемый файл можно запустить напрямую:./main   # В Linux/macOS
main.exe # В Windows

Пример программы на Go:

Рассмотрим программу, которая выполняет базовые вычисления.

Исходный код (`main.go`):
package main

import (
    "fmt"
    "math"
)

func main() {
    fmt.Println("Welcome to Go!")

    // Пример работы с числами
    a, b := 5, 10
    sum := a + b
    fmt.Printf("Sum of %d and %d is %d\n", a, b, sum)

    // Использование функции из пакета math
    squareRoot := math.Sqrt(16)
    fmt.Printf("Square root of 16 is %.2f\n", squareRoot)

    // Логика с условием
    if a < b {
        fmt.Println("a is less than b")
    } else {
        fmt.Println("a is greater than or equal to b")
    }
}

Сборка и запуск:
1. Компиляция:
     go build main.go
      Исполняемый файл будет создан в текущем каталоге.
2. Запуск:
     ./main
      Вывод:
     Welcome to Go!
   Sum of 5 and 10 is 15
   Square root of 16 is 4.00
   a is less than b

Итоги:
1. Исходный код: Программа на Go организована в файлах с расширением . 
go, имеет модульную структуру и требует строгого форматирования.

2. Сборка программы: Выполняется командой go build, создавая исполняемый файл. Go также поддерживает кросс-компиляцию.

3. Запуск программы: Можно использовать команду go run для быстрого запуска или исполняемый файл для окончательной версии программы.
*/
package main