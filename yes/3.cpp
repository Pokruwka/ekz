/*Типы данных в С++
1. Типизация в C++
Что такое типизация?
Типизация — это способ организации данных в программе, определяющий:
- Какие значения переменная может принимать.
- Какие операции над переменной допустимы.

Виды типизации
Статическая типизация (используется в C++)
- Тип переменной указывается во время компиляции и не может измениться.
- Пример: int x = 10; // x — переменная типа int, и её тип не изменится.
  
Преимущества:
- Обнаружение ошибок на этапе компиляции.
- Более эффективная работа программы.

Динамическая типизация
- Тип переменной определяется во время выполнения программы.
- Не используется в C++, но применяется в языках вроде Python.

Зачем нужна типизация?
1. Для управления памятью.
   - Тип переменной определяет, сколько памяти она занимает (например, int занимает 4 байта).
2. Для безопасности.
   - Ограничивает выполнение недопустимых операций (например, нельзя умножать строку на число).
3. Для оптимизации.
   - Компилятор может оптимизировать код, зная типы переменных.

2. Простые типы данных
Простые типы данных в C++ делятся на:
1. Целые числа.
2. Вещественные числа.
3. Символьный тип.
4. Логический тип.

Целые числа (int)
- Используются для хранения целых чисел.
- Разные модификаторы определяют диапазон значений:
  - short (обычно 2 байта).
  - int (обычно 4 байта).
  - long (обычно 4–8 байт).
  - long long (обычно 8 байт).

Вещественные числа (float, double)
- Используются для хранения чисел с плавающей запятой.
- Основные типы:
  - float (4 байта, точность ~6 знаков).
  - double (8 байт, точность ~15 знаков).
  - long double (более высокая точность, от 8 байт и выше).

Символьный тип (char)
- Хранит один символ.
- Обычно занимает 1 байт.

Логический тип (bool)
- Хранит логические значения: true или false.
- Занимает 1 байт.

3. Составные типы данных
Составные типы данных позволяют хранить и организовывать сложные данные.
Массивы
- Набор однотипных элементов, хранящихся в памяти последовательно.
- Размер массива задаётся при создании.

Перечисления (enum)
- Набор именованных констант.
Пример:
enum Color { RED, GREEN, BLUE };
Color myColor = RED;

Структуры (struct)
- Позволяют объединить данные разных типов в одном объекте.

 Классы (class)
- Расширение структур, поддерживают методы и модификаторы доступа (`private`, `public`).

 Строки
- Представлены классом std::string в библиотеке <string>.

Динамические структуры данных
- Используют указатели и выделение памяти:
  - Списки.
  - Деревья.
  - Очереди.
Пример выделения памяти:
int* ptr = new int(10); // Динамическая переменная
delete ptr;             // Освобождение памяти

 4. Преобразование типов

Неявное преобразование
- Происходит автоматически, если типы совместимы.
- Например, при присвоении значения меньшего типа большему:
   int x = 10;
  double y = x; // Неявное преобразование int -> double

Явное преобразование
- Выполняется вручную с помощью операторов или функций.
- Используется, если типы не совместимы.

Пример:
double value = 10.5;
int roundedValue = (int)value; // Явное преобразование double -> int

С помощью функций:
float x = 3.14f;
int y = static_cast<int>(x); // Предпочтительный способ

Разница между явным и неявным преобразованием
-  Неявное безопасно и выполняется автоматически, но может привести к потере данных.
- Явное дает полный контроль, но требует осторожности.
Итог:
1. Типизация в C++ статическая, определяет, какие значения может принимать переменная.
2. Простые типы данных включают целые, вещественные, символы и логические значения.
3. Составные типы данных позволяют хранить сложные структуры: массивы, структуры, классы.
4. Преобразование типов бывает неявным (автоматическим) и явным (вручную).
*/