/*Переменные и константы в языке Golang
Golang (Go) предоставляет удобные средства для работы с переменными и константами, которые являются основой управления данными в программах. Рассмотрим эти понятия подробно.

1. Константы

Константы — это неизменяемые значения, которые задаются на этапе компиляции и не могут быть изменены в ходе выполнения программы. В Go они используются для хранения значений, которые остаются неизменными (например, математических констант, фиксированных настроек).

Создание констант
Для объявления констант используется ключевое слово const.

Пример создания:
package main

import "fmt"

func main() {
    const pi = 3.14             // Числовая константа
    const appName = "MyApp"     // Строковая константа
    const isEnabled = true      // Логическая константа

    fmt.Println("Pi:", pi)
    fmt.Println("App Name:", appName)
    fmt.Println("Is Enabled:", isEnabled)
}

Особенности:
1. Типизация:
   - Константы могут быть типизированными:
         const a int = 42
        - Или не типизированными (Go автоматически определяет тип на основе использования):
         const b = 42 // b считается числом
     
2. Множественное объявление:
   - Можно объявлять несколько констант одновременно:
         const (
         x = 10
         y = 20
         z = "Hello"
     )
     
3. Компиляция и неизменяемость:
   - Константы определяются во время компиляции, их нельзя изменять.
   - Ошибка:
         const x = 10
     x = 20 // Ошибка: нельзя изменить значение константы
     
4. Выражения в константах:
   - Константы могут быть результатом вычисления выражений, если все их части — это константы:
         const radius = 10
     const circumference = 2 * pi * radius
2. Переменные

Переменные — это изменяемые контейнеры для хранения данных. В Go они могут быть различных типов, включая базовые (числа, строки) и сложные (слайсы, карты и т.д.).

Создание переменных

Для объявления переменных в Go используются три основные формы:

1. Использование ключевого слова `var`:
var name string // Объявление переменной с типом
name = "John"   // Присвоение значения

2. Одновременное объявление и инициализация:
var age int = 30 // Указание типа
var isStudent = false // Тип определяется автоматически

3. Краткая форма (`:=`):
- Используется внутри функций для упрощённой записи:message := "Hello, Go!"
count := 10

#### Множественное объявление:
var (
    a int
    b string
    c bool
)

Использование переменных

Пример работы с переменными:
package main

import "fmt"

func main() {
    var name string = "Alice"
    age := 25
    fmt.Printf("Name: %s, Age: %d\n", name, age)

    // Изменение значения переменной
    age = 26
    fmt.Printf("Updated Age: %d\n", age)}
Особенности переменных:
1. Объявление без инициализации:
   - Переменной будет присвоено нулевое значение для её типа:
     - Для чисел: 0.
     - Для строк: пустая строка "".
     - Для булевых значений: false.
         var x int  // x == 0
     var y bool // y == false
     
2. Область видимости:
   - Переменные, объявленные с var, могут быть глобальными или локальными.
   - Переменные, объявленные с :=, доступны только в пределах текущей функции.

Сложные типы переменных:

Слайсы (Slices)

Слайс — это динамический массив. Его длина может изменяться во время выполнения.

Объявление и использование:
package main

import "fmt"

func main() {
    var nums []int            // Пустой слайс
    nums = append(nums, 1, 2) // Добавление элементов
    fmt.Println(nums)         // [1, 2]

    // Инициализация слайса
    fruits := []string{"Apple", "Banana", "Cherry"}
    fmt.Println(fruits)

    // Изменение значения
    fruits[1] = "Blueberry"
    fmt.Println(fruits) // [Apple Blueberry Cherry]
}

Особенности слайсов:
1. Слайсы ссылаются на массивы в памяти.
2. Поддерживают операции:
   - Добавление элементов: append(slice, elements...).
   - Получение длины и ёмкости: len(slice) и cap(slice).

Словари (Maps):

Словарь — это коллекция пар ключ-значение.

Объявление и использование
package main

import "fmt"

func main() {
    // Создание словаря
    grades := map[string]int{
        "Alice": 90,
        "Bob":   85,
    }
    fmt.Println(grades) // map[Alice:90 Bob:85]

    // Добавление нового значения
    grades["Charlie"] = 95

    // Доступ по ключу
    fmt.Println("Alice's grade:", grades["Alice"])

    // Проверка существования ключа
    value, exists := grades["Bob"]
    if exists {
        fmt.Println("Bob's grade:", value)
    } else {
        fmt.Println("Bob is not in the map")
    }

    // Удаление элемента
    delete(grades, "Alice")
    fmt.Println(grades) // map[Bob:85 Charlie:95]
}
Особенности словарей:
1. Ключи должны быть сравнимыми типами (например, числа, строки).
2. Значения могут быть любыми типами, включая сложные (например, слайсы).

Итоги
1. Константы:
   - Используются для неизменяемых данных.
   - Объявляются с помощью const и не могут быть изменены.
2. Переменные:
   - Создаются с использованием var или :=.
   - Поддерживают широкий спектр типов данных, включая сложные (слайсы, словари).
3. Слайсы:
   - Динамические массивы с гибкой длиной.
   - Используются для хранения списка данных.
4. Словари:
   - Ассоциативные массивы (ключ-значение).
   - Идеальны для быстрого поиска данных по ключу.
*/
package main