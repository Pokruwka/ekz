/*ООП в С++. Наследование
Наследование — один из ключевых принципов объектно-ориентированного программирования (ООП), позволяющий создавать новые классы на основе существующих. Этот механизм позволяет переиспользовать код, расширять функциональность и создавать иерархию классов.

1. Основная идея принципа наследования

Цель наследования:
1. Повторное использование кода:  
   Общие свойства и методы базового класса могут быть использованы в производных классах.
2. Создание иерархий:  
   Организация классов в древовидную структуру для удобного описания и использования взаимосвязанных объектов.
3. Расширяемость:  
   Производные классы могут дополнять или изменять функциональность базовых.

Ключевые особенности:
- Производный (дочерний) класс наследует поля и методы базового (родительского) класса.
- Производный класс может дополнять или переопределять поведение базового класса.
- Наследование позволяет описывать общие черты (в базовом классе) и различия (в производных).

2. Основные понятия

Базовый класс (Base class):
Класс, от которого наследуют другие классы. Он содержит общие данные и методы.

Производный класс (Derived class):
Класс, который наследует базовый класс. Он может:
- Использовать методы и поля базового класса.
- Дополнять базовый класс своими полями и методами.
- Переопределять поведение методов базового класса.

Прямое и многоуровневое наследование:
- Прямое наследование:  
  Производный класс наследует только один базовый класс.  
   class Base {};
  class Derived : public Base {};
  - Многоуровневое наследование:  
  Производный класс становится базовым для другого класса.  
   class A {};
  class B : public A {};
  class C : public B {};
  
Виды наследования (модификаторы доступа):
1. public:  
   Общедоступные и защищённые члены базового класса остаются доступными в производном классе.
2. protected:  
   Общедоступные члены базового класса становятся защищёнными в производном классе.
3. private:  
   Все унаследованные члены базового класса становятся приватными в производном классе.

---

3. Объявление наследования

### Синтаксис:
class DerivedClass : AccessSpecifier BaseClass {
    // Поля и методы производного класса
};

- DerivedClass — производный класс.
- AccessSpecifier — модификатор доступа (`public`, protected, `private`).
- BaseClass — базовый класс.

### Пример:
class Animal {
protected:
    string name;

public:
    Animal(string n) : name(n) {}
    void speak() {
        cout << name << " makes a sound." << endl;
    }
};

class Dog : public Animal {
public:
    Dog(string n) : Animal(n) {}
    void speak() {
        cout << name << " barks." << endl; // Переопределение метода
    }
};

4. Использование наследования

Преимущества использования:
1. Переиспользование кода:  
   Поля и методы базового класса доступны в производном.
2. Расширяемость:  
   Производный класс может добавлять новые функции, дополняя базовый.
3. Переопределение:  
   Производный класс может изменить поведение методов базового класса.

Примеры:

Пример 1: Прямое наследование
class Vehicle {
public:
    void start() {
        cout << "Vehicle started" << endl;
    }
};

class Car : public Vehicle {
public:
    void drive() {
        cout << "Car is driving" << endl;
    }
};

int main() {
    Car myCar;
    myCar.start(); // Метод базового класса
    myCar.drive(); // Метод производного класса
    return 0;
}

Пример 2: Многоуровневое наследование
class A {
public:
    void methodA() {
        cout << "Method A" << endl;
    }
};

class B : public A {
public:
    void methodB() {
        cout << "Method B" << endl;
    }
};

class C : public B {
public:
    void methodC() {
        cout << "Method C" << endl;
    }
};

int main() {
    C obj;
    obj.methodA();
    obj.methodB();
    obj.methodC();
    return 0;
}

---

Полиморфизм и виртуальные функции
Полиморфизм позволяет производному классу изменять поведение базового. Для этого используется ключевое слово virtual.

Пример:
class Shape {
public:
    virtual void draw() {
cout << "Drawing shape" << endl;
    }
};

class Circle : public Shape {
public:
    void draw() override {
        cout << "Drawing circle" << endl;
    }
};

int main() {
    Shape* shape = new Circle();
    shape->draw(); // Вызывает метод Circle::draw()
    delete shape;
    return 0;
}


Области видимости и доступ к членам базового класса
Protected члены:
- Доступны производным классам, но не объектам.

Private члены:
- Не доступны производным классам напрямую.

Пример:
class Parent {
protected:
    int protectedVar;

private:
    int privateVar;
};

class Child : public Parent {
public:
    void accessProtected() {
        protectedVar = 10; // Доступно
        // privateVar = 20; // Ошибка: недоступно
    }
};

Итоги:
1. Наследование позволяет создавать классы на основе других, повторно используя код.
2. Производные классы могут дополнять или переопределять функциональность базовых.
3. В C++ поддерживаются разные виды наследования: прямое, многоуровневое, множественное.
4. Полиморфизм и виртуальные функции усиливают гибкость наследования.
*/