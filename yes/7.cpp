/*Функции в языке С++

1. Объявление функций

Что такое объявление функции?
Объявление функции (или прототип функции) сообщает компилятору о существовании функции до её вызова. Это включает в себя:
- Имя функции.
- Тип возвращаемого значения.
- Список параметров (тип и, опционально, имена).

Синтаксис:
<тип_возвращаемого_значения> имя_функции(параметры);

Пример:
int add(int a, int b); // Объявление функции

- Зачем нужно?
  Если вызов функции происходит до её определения, то компилятор должен знать её сигнатуру (имя, параметры и возвращаемый тип).

Пример с использованием объявления:
#include <iostream>
using namespace std;

int add(int a, int b); // Объявление

int main() {
    cout << add(3, 5); // Вызов функции
    return 0;
}

int add(int a, int b) { // Определение
    return a + b;
}

2. Определение функций

 Что такое определение функции?
Определение функции — это реализация (тело) функции, где указывается, что именно она делает.

Синтаксис:
<тип_возвращаемого_значения> имя_функции(параметры) {
    // Тело функции
}

- Тип возвращаемого значения:
  Определяет, какой тип данных функция возвращает. Если функция ничего не возвращает, используется тип void.

- Параметры:
  Переменные, передаваемые в функцию для обработки.

Виды функций:

1. Функции, возвращающие значение:
   - Возвращают результат выполнения с помощью оператора return.

   Пример:
     int square(int x) {
       return x * x;
   }
   
2. Функции без возвращаемого значения (`void`):
   - Выполняют действия, но не возвращают значения.

   Пример:
     void greet() {
       cout << "Hello, World!" << endl;
   }
   
3. Функции с параметрами и без:
   - Параметры позволяют передавать значения в функцию.
   - Функция без параметров:
         void sayHello() {
         cout << "Hello!" << endl;
     }
     
4. Рекурсивные функции:
   - Функции, которые вызывают сами себя.
   - Используются для задач, которые можно разбить на подзадачи.

   Пример:
     int factorial(int n) {
       if (n == 0) return 1;
       return n * factorial(n - 1);
   }
   

3. Использование функций

Функции в C++ используются для:
- Разделения кода на модули.
- Повторного использования кода.
- Улучшения читаемости и сопровождения.

Вызов функции
Функция вызывается по её имени с передачей аргументов, если это требуется.

Синтаксис:
имя_функции(аргументы);

Передача аргументов в функцию

1. Передача по значению (by value):
   - Функция получает копию значения, оригинал не изменяется.
   Пример:
     void increment(int a) {
       a++;
   }
   
2. Передача по ссылке (by reference):
   - Функция работает с оригиналом, изменения сохраняются.
   Пример:
     void increment(int& a) {
       a++;
   }
   
3. Передача указателя:
   - Передача адреса переменной.
   Пример:
     void increment(int* a) {
       (*a)++;
   }

4. Функция `main`

Особенность функции `main`
Функция main — это главная функция программы. Она определяет точку входа и выхода программы.

Возвращаемое значение
- 0: Успешное завершение программы.
- Любое другое значение может указывать на ошибку.

Аргументы функции `main`:

int main(int argc, char* argv[]) {
    // argc — количество аргументов командной строки.
    // argv — массив строковых аргументов.
}

Пример использования:
#include <iostream>
using namespace std;

int main(int argc, char* argv[]) {
    cout << "Program name: " << argv[0] << endl;
    if (argc > 1) {
        cout << "Argument: " << argv[1] << endl;
    }
    return 0;
}

Итоги
1. Объявление функций позволяет использовать их до определения.
2. Определение функции включает реализацию её логики.
3. Использование функций делает код читаемым, модульным и повторно используемым.
4. Функция `main` — точка входа в программу, откуда начинается её выполнение.
*/