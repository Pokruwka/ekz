/*Программы на языке С++

1.	Что такое исходный код и как он работает?
Исходный код — это текст программы, написанный на языке программирования, в данном случае на языке C++. Этот текст содержит инструкции, которые описывают логику работы программы.

- Структура исходного кода:
  - Включение библиотек (например, `#include <iostream>`): указывает компилятору на использование внешних функций и классов.
  - Объявление функций и классов: определяет, какие действия будет выполнять программа.
  - Точка входа: функция main(), с которой начинается выполнение программы.

- Как он работает?
  Исходный код — это человекочитаемая форма программы. Компьютер, однако, работает с машинным кодом (последовательностью двоичных инструкций). Поэтому перед запуском исходный код должен быть преобразован в машинный код.

 2. Как происходит сборка программы?

Сборка программы — это процесс преобразования исходного кода в исполняемый файл. Она включает несколько этапов:

1. Препроцессор:
   - Выполняет команды, начинающиеся с # (например, #include, `#define`).
   - Вставляет содержимое подключаемых файлов (`#include`) в основной код.
   - Удаляет комментарии и разворачивает макросы.

2. Компиляция:
   - Исходный код на C++ преобразуется в объектный код (файлы с расширением .o или `.obj`).
   - Объектный код — это машинный код, но он ещё не готов к исполнению, поскольку не содержит ссылок на внешние библиотеки.

3. Линковка (связывание):
   - Объектный код соединяется с библиотеками (например, стандартной библиотекой C++).
   - Результатом является исполняемый файл (например, .exe на Windows или без расширения на Linux).

4. Оптимизация (необязательно):
   - Компилятор может улучшать код, чтобы он работал быстрее или занимал меньше памяти.

Инструменты:
- Для компиляции используют компиляторы, например:
  - GCC (GNU Compiler Collection).
  - Clang.
  - Microsoft Visual C++.

3. Как происходит запуск программы?

После компиляции и линковки создаётся исполняемый файл, который можно запустить. 

1. Процесс запуска:
   - Операционная система загружает исполняемый файл в память.
   - Создаётся процесс (instance) программы.
   - Программа начинает выполнение с функции main().

2. Что происходит во время работы программы?
   - Код программы выполняется последовательно, начиная с точки входа.
   - Если программа вызывает функции, они выполняются и возвращают результат.
   - Программа может взаимодействовать с пользователем (ввод/вывод данных), файлами, сетью и т.д.
3. Результат работы программы:
   - Результатом может быть:
     - Вывод данных на экран (например, через `std::cout`).
     - Запись в файл.
     - Обработка пользовательского ввода.
 Итог:

1. Исходный код — текст программы, который программист пишет на C++.

2. Сборка программы включает этапы предобработки, компиляции и линковки, которые преобразуют код в исполняемый файл.

3. Запуск программы — это выполнение исполняемого файла, который ОС загружает в память.




*/