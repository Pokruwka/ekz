/*Работа с идентификаторами в C+
Идентификаторы играют ключевую роль в программировании на C++, так как именно они используются для именования переменных, функций, классов и других сущностей. Рассмотрим каждый из пунктов подробно.

1. Допустимые идентификаторы в C++

Что такое идентификатор?
Идентификатор — это имя, которое программист присваивает переменным, функциям, классам, пространствам имён и другим элементам программы.

Правила формирования идентификаторов:
1. Допустимые символы:
   - Буквы (`a-z`, `A-Z`), цифры (`0-9`) и символ подчеркивания (`_`).
   - Имя идентификатора не может начинаться с цифры.

2. Длина идентификатора:
   - Ограничений на длину идентификатора в C++ нет, но слишком длинные идентификаторы ухудшают читаемость.

3. Регистрозависимость:
   - Идентификаторы в C++ чувствительны к регистру (`variable` и Variable — разные идентификаторы).

4. Ключевые слова:
   - Ключевые слова языка C++ нельзя использовать как идентификаторы. Например, нельзя назвать переменную int, class, return и т.д.

5. Стандартные соглашения:
   - Переменные обычно именуются с использованием строчных букв (`myVariable`).
   - Константы — заглавными (`PI`, `MAX_VALUE`).
   - Часто используется snake_case или camelCase для именования.

Пример допустимых идентификаторов:
int myVariable;
float _price;
char user_name;

Пример недопустимых идентификаторов:
int 2ndVariable; // Ошибка: начинается с цифры
float return;    // Ошибка: ключевое слово
char user-name;  // Ошибка: запрещённый символ "-"

2. Пространства имён

Что такое пространство имён?
Пространства имён (`namespace`) в C++ используются для группировки идентификаторов и предотвращения конфликтов имён.

Синтаксис пространства имён:
namespace имя_пространства_имён {
    // Объявления и определения
}

Пример использования:
#include <iostream>
using namespace std;

namespace Math {
    int add(int a, int b) {
        return a + b;
    }
}

int main() {
    int result = Math::add(3, 5); // Доступ через оператор :: (scope resolution)
    cout << "Result: " << result << endl;
    return 0;
}

Типы пространств имён:
1. Явное использование (`namespace::identifier`):
   - Вызываются с помощью оператора разрешения области видимости (`::`).

2. Подключение пространства имён (`using`):
   - Можно подключить пространство имён, чтобы избежать явного указания:
         using namespace std;
     
3. Вложенные пространства имён:
   - Пространства имён могут быть вложенными:
         namespace Outer {
         namespace Inner {
             int value = 10;
         }
     }
     
4. Анонимные пространства имён:
   - Используются для идентификаторов, видимых только в текущем файле.
         namespace {
         int localVariable = 42;
     }

3. Области видимости (Scope)

Что такое область видимости?
Область видимости определяет, где можно использовать идентификатор в программе.

Типы областей видимости:

1. Глобальная область видимости:
   - Переменные, объявленные вне всех функций или классов.
   - Видны во всей программе.

   Пример:
     int globalVar = 10;

   int main() {
       cout << globalVar; // Доступ к глобальной переменной
       return 0;
   }
   
2. Локальная область видимости:
   - Переменные, объявленные внутри функций или блоков кода.
   - Доступны только в пределах их блока.

   Пример:
     int main() {
       int localVar = 20; // Локальная переменная
       cout << localVar;
       return 0;
   }
   
3. Область видимости блоков:
   - Переменные, объявленные в {}, видны только внутри этих фигурных скобок.

   Пример:
     if (true) {
       int blockVar = 30; // Видна только внутри блока if
   }
   
4. Область видимости в классах и структурах:
   - Переменные-члены класса видны только через объекты класса (если они не являются `static`).

   Пример:
     class MyClass {
       int privateVar; // Приватная переменная
   public:
       int publicVar;
   };
   
5. Статическая область видимости:
   - Используется для переменных, которые сохраняют своё значение между вызовами функций.

   Пример:
void func() {
       static int count = 0;
       count++;
       cout << count << endl;
   }
   
4. Связывание идентификатора с программной сущностью (Binding)

Что такое binding?
Binding — это процесс связывания идентификатора с конкретной сущностью (переменной, функцией или другим объектом).

Виды связывания:
1. Раннее связывание (Static Binding):
   - Связывание идентификатора происходит на этапе компиляции.
   - Используется для обычных функций и переменных.

   Пример:
     void greet() {
       cout << "Hello!" << endl;
   }

   int main() {
       greet(); // Связывание происходит на этапе компиляции
       return 0;
   }
   
2. Позднее связывание (Dynamic Binding):
   - Связывание происходит на этапе выполнения программы.
   - Используется в полиморфизме с виртуальными функциями.

   Пример:
     class Base {
   public:
       virtual void show() {
           cout << "Base class" << endl;
       }
   };

   class Derived : public Base {
   public:
       void show() override {
           cout << "Derived class" << endl;
       }
   };

   int main() {
       Base* ptr;
       Derived obj;
       ptr = &obj;
       ptr->show(); // Позднее связывание
       return 0;
   }

Итоги
1. Допустимые идентификаторы должны следовать правилам языка, избегая ключевых слов и конфликтов.
2. Пространства имён помогают группировать идентификаторы и избегать их конфликтов.
3. Области видимости определяют доступность идентификаторов в разных частях программы.
4. Binding связывает идентификаторы с сущностями, причём связывание может быть ранним (компиляция) или поздним (исполнение).
*/